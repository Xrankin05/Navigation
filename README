Create workspace
Install GIT if you haven't for vscode integration
Pull code
Create Venv (ctrl+shift+p, Python create environment, venv)
Select venv (in terminal ./venv/Scripts/activate)
Install required libraries (pip install PyQT5)
